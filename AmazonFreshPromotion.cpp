#include <bits/stdc++.h>
using namespace std;

int main() {
	int n; cin >> n;
	vector<string> shoppingCart(n);
	for(int i = 0; i < n; i++) cin >> shoppingCart[i];
    int m; cin >> m;
    vector<vector<string> >  codeList(m);
	for(int i = 0; i < m; i++) {
		int len; cin >> len;
	}
	return 0;
}